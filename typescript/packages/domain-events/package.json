{
  "name": "@apso/domain-events",
  "version": "0.1.0",
  "description": "Durable domain-event spine (transactional outbox + runtime-selected delivery) for NestJS + TypeORM.",
  "license": "MIT",
  "author": "Apso",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "jest",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "@nestjs/common": ">=9 <12",
    "@nestjs/core": ">=9 <12",
    "@nestjs/typeorm": ">=9 <12",
    "rxjs": ">=7",
    "typeorm": ">=0.3"
  },
  "optionalDependencies": {
    "@aws-sdk/client-eventbridge": "^3.0.0",
    "@aws-sdk/client-sqs": "^3.0.0",
    "@nestjs/microservices": "^10.0.0",
    "kafkajs": "^2.0.0"
  },
  "devDependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/typeorm": "^10.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "typeorm": "^0.3.20"
  },
  "keywords": [
    "apso",
    "domain-events",
    "outbox",
    "nestjs",
    "typeorm"
  ]
}
